# Copy to keystore.properties and fill in after create-keystore (see README).
# Paths are relative to this playstore/ directory unless absolute.

storeFile=rydo-upload.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=rydo-upload
keyPassword=YOUR_KEY_PASSWORD
