# Copy to `.env.local` and fill in values used by the Vite client.

VITE_API_MODE=real
VITE_API_BASE_URL=http://localhost:5000

# Required for `/live` Mapbox GL (get a token at https://account.mapbox.com/)
VITE_MAPBOX_ACCESS_TOKEN=
