/** Desktop in-flow action bar (opaque strip). Mobile uses {@link MobileFloatingActions} — no background on the CTA slot. */
export const desktopChromeFooterClass =
  'relative z-[3] flex shrink-0 items-center gap-2.5 border-t border-border/60 bg-[var(--rydo-bg-deep)]/90 px-5 py-3.5 backdrop-blur-xl';
