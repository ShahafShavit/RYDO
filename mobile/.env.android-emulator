# Android Emulator — host loopback via 10.0.2.2 (not 127.0.0.1)
# Applied by: npm run env:android  →  writes mobile/.env.local
# Vite mode (when scaffold exists): vite build --mode android-emulator

VITE_API_MODE=real
VITE_API_BASE_URL=http://10.0.2.2:5000
VITE_PLATFORM=native

VITE_MAPBOX_ACCESS_TOKEN=

# VITE_LOG_RIDE_LIVE=true
